config:
  target: "http://localhost:8000/api/v1"
  phases:
    - duration: 60 # Duración de la prueba en segundos
      arrivalRate: 10 # 10 nuevos usuarios virtuales por segundo
scenarios:
  - name: "Health Check"
    flow:
      - get:
          url: "/health"